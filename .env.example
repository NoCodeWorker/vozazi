# VOZAZI Platform - Root Environment Variables
# These are optional environment variables for the monorepo root

# npm registry (optional)
# NPM_CONFIG_REGISTRY=https://registry.npmjs.org

# Turbo cache directory (optional)
# TURBO_CACHE_DIR=.turbo
